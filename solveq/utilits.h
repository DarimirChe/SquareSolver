#ifndef UTILITS_H
#define UTILITS_H

const double PRECISION = 1e-5; // calculations are performed with an accuracy of 5 decimal places

bool isEqual(double firstNumber, double secondNumber, double precision);
bool isZero (double comparedNumber, double precision);

#endif